// allows default import: import Button from '@ui/Button';
export { default } from './Button';
// allows named import: import { Button } from '@ui/Button';
export { default as Button } from './Button';
